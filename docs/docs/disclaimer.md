---
sidebar_position: 2
title: Disclaimer
---

# Disclaimer

## CabbageSign does NOT work — and that's by design.

CabbageSign is **not** a real sideloading application. It cannot sign IPAs, install apps, bypass app restrictions, or interact with Apple's signing infrastructure in any way. Attempting to use it as a sideloading tool will not work.

The project exists purely as a **UI/UX template** for developers who want to build a sideloading app that looks polished from day one. All signing logic, network calls, and package management are left intentionally unimplemented — it is your job to fill those in.

Additionally, CabbageSign was **not developed inside Xcode**. The project was authored with standard text editors and compiled exclusively via GitHub Actions, which generates the `.ipa` artifact.
